import 'package:flutter/material.dart';

class UtilsCommon {
  static List<Map<String, dynamic>> get languages => [
        {'locale': const Locale('en'), 'name': 'English'},
        {'locale': const Locale('hr'), 'name': 'Croatian'},
      ];
}
